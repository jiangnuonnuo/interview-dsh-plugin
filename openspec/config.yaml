schema: spec-driven

context: |
  Language: zh
  All artifacts must be written in zh.
  Keep OpenSpec structural headings and SHALL/MUST keywords in English.

  interview-dsh is a DeepSeek Harness (DSH) plugin, not a standalone app.
  Product: docs/product/REQUIREMENTS.md.
  Product release process: docs/product/DISTRIBUTION.md.
  Architecture: docs/architecture/ARCHITECTURE.md.
  Coding constraints: AGENTS.md.
  Interviewer behavior: interviewer-role-prompt.md.
  Forbidden approaches (do not retry): docs/FORBIDDEN.md.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
# Example:
#   operations:
#     apply:
#       guidance:
#         - Keep test summaries concise
#     archive:
#       guidance:
#         - Summarize the archive outcome before finishing
